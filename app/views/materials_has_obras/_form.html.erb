<%= form_with(model: materials_has_obra) do |form| %>
  <% if materials_has_obra.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(materials_has_obra.errors.count, "error") %> prohibited this materials_has_obra from being saved:</h2>

      <ul>
        <% materials_has_obra.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :material_id, style: "display: block" %>
    <%= form.text_field :material_id %>
  </div>

  <div>
    <%= form.label :brand_id, style: "display: block" %>
    <%= form.text_field :brand_id %>
  </div>

  <div>
    <%= form.label :cantidad, style: "display: block" %>
    <%= form.number_field :cantidad %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
